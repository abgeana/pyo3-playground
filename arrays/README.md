# arrays

It might be better to use a virtual environment to run this example. The steps are:

1. install the module using `pip install .`
2. run the test script `python test.py`

The following output should appear:

```
got a python list with length = 5
got a numpy ndarray with shape[0] = 5
```
